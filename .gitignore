# Binaries
/bin/
/dist/
*.exe
*.test
*.out

# Go workspace
go.work
go.work.sum

# Editor / OS
.DS_Store
.idea/
.vscode/
*.swp

# Local config
.env
.env.*
!.env.example

# Coverage
coverage.out
coverage.html

# Loadgen output (default --out path; per-run manifests should not land in VCS)
manifest.json
manifest-*.json

# Run artifacts (Session 4) and e2e workflow artifacts (Session 7).
/runs/
/e2e/

# Stale doc-snapshot zips (every loose .md/.pdf in docs/ is the source of truth; zip backups pollute git status — see CLAUDE.md drift entry 2026-05-22)
docs/*.zip
runs/sanity/
